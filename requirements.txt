# clang==16.0.6               # libclang Python bindings for parsing and rewriting C code
# numpy==1.26.4               # for basic array operations / validation
# pandas==2.2.2               # for logging benchmark data
# matplotlib==3.9.2           # for plotting performance comparison graphs
# tabulate==0.9.0             # for clean console reporting
# psutil==6.0.0               # for measuring CPU utilization / energy proxy
clang               # libclang Python bindings for parsing and rewriting C code
numpy               # for basic array operations / validation
pandas               # for logging benchmark data
matplotlib           # for plotting performance comparison graphs
tabulate             # for clean console reporting
psutil
#optional
# autopep8==2.0.4
# black==24.4.2
