"""
Executor package — compiles, runs, and profiles C programs.

Modules:
- compiler_runner.py  → Handles GCC/Clang compilation.
- benchmark_runner.py → Executes binaries, measures runtime, stores results.
- power_profiler.py   → Optional: tracks CPU power usage (Linux only).
"""
